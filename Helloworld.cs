using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Helloworld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       //Обычный коментарий:)
       print("Проверка консоли"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
